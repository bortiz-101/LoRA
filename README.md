# COMP 488: LLMS Final Project

Create python virtual environment. The script will create a .venv directory and install all requirments from `requirements.txt`

```bash
source ./setup.sh
```

The training pipeline can be run optionally with jupyter notebook `train.ipynb` (cell by cell) or run with the `train.py` file

```bash
python3 train.py
```